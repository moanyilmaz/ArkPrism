import { geoLocationManager } from '@kit.LocationKit';
// geoLocationManager.getAddressesFromLocation()
export function execute(): void {
  const text = 'geoLocationManager.getAddressesFromLocation';
  console.info(text);
}
